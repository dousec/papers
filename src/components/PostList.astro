---
import type { CollectionEntry } from "astro:content";
import FormattedDate from "./FormattedDate.astro";
import { tagPath } from "../utils/content";
import { withBase } from "../utils/withBase";

interface Props {
	posts: CollectionEntry<"papers">[];
	showDescription?: boolean;
}

const { posts, showDescription = true } = Astro.props;
---

<ul class="post-list">
  {
    posts.map((post) => (
      <li class="post-list-item">
        <time class="post-list-date" datetime={post.data.pubDate.toISOString()}>
          <FormattedDate date={post.data.pubDate} short={true} />
        </time>
        <article class="post-list-body">
          <h2 class="post-list-title"><a href={withBase(`/papers/${post.id}/`)}>{post.data.title}</a></h2>
          {showDescription && <p>{post.data.description}</p>}
          <footer class="post-list-meta">
            <div class="post-list-tags">
              {(post.data.tags ?? []).map((tag) => (
                <a href={withBase(tagPath(tag))}>#{tag}</a>
              ))}
            </div>
          </footer>
        </article>
      </li>
    ))
  }
</ul>
